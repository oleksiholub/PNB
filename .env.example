NODE_ENV=production
PORT=8080

# Deployment region constraint per TZ section 1/3.2/6 (us-east1 mandatory for Cloud Run defaults)
GCP_REGION=us-east1
GCP_PROJECT_ID=REPLACE_ME

FIREBASE_PROJECT_ID=REPLACE_ME

# GitHub App (private key path resolved via Secret Manager at runtime, added in Iteration F)
GITHUB_APP_ID=REPLACE_ME
GITHUB_INSTALLATION_ID=REPLACE_ME